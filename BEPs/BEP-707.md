```
BEP: 707
Title: Post-Quantum Cryptography Migration Roadmap for BNB Chain
Status: Draft
Type: Information
Created: 2026-08-11
Description(optional): Umbrella proposal describing the overall, multi-BEP, phased roadmap for migrating BNB Chain to post-quantum cryptography. Concrete technical specifications are delegated to companion Standards BEPs.
```

# BEP: Post-Quantum Cryptography Migration Roadmap for BNB Chain

## 1. Summary

This BEP is the umbrella roadmap for making BNB Chain resistant to attacks from large-scale quantum computers. It describes the quantum threat, sets shared guiding principles, and defines a three-phase plan. It is the first of a **series of BEPs**: this document is intentionally high-level, and each phase will be specified in detail by its own companion Standards BEP. It also serves as the **tracking index for the whole BNB Chain family** — BSC, opBNB, BNB Greenfield, and the new chain under development each maintain their own migration roadmap against these phases, indexed and status-tracked in [Section 5](#5-per-chain-migration-tracking).

## 2. Abstract

Once available, large-scale quantum computers running Shor's algorithm will break the public-key cryptography that secures BNB Chain today — elliptic-curve account signatures, pairing-based cryptography in consensus and data availability, and node-to-node key exchange. No such machine exists yet, and mainstream estimates place its arrival ("Q-Day") between **2029 and 2035**, but migrating a chain with hundreds of millions of accounts and a large ecosystem takes years, so preparation must begin now.

The full migration is deliberately structured as **multiple BEPs delivered in phases**, not a single monolithic change. This umbrella BEP (Information type) covers the strategy only; the technical specifications live in companion Standards BEPs, one (or more) per phase:

- **Phase 1 — P2P key-exchange upgrade**
- **Phase 2 — ECDSA account-signature upgrade**
- **Phase 3 — Pairing-based cryptography upgrade** (BLS aggregate signatures, KZG commitments, BN254 pairing precompiles)

![[pqc_roadmap]](./assets/BEP-707/pqc_roadmap.png)
Scope here is the **core protocol layer of every chain in the BNB Chain family** — BSC, opBNB, BNB Greenfield, and the new chain currently under development. The three phases define the shared direction and principles; each chain has a different cryptographic surface, release cadence, and governance path, so each maintains its own migration roadmap anchored to this document (see [Section 5](#5-per-chain-migration-tracking)). Application-ecosystem migration (bridges, stablecoins, DAOs, wallets, etc.) is addressed as a coordination responsibility, since chain-level quantum resistance does not by itself protect application-layer cryptography.

## 3. Motivation

### The quantum threat

Two quantum algorithms matter:

- **Shor's algorithm** solves integer factorization and discrete logarithms in polynomial time. Every deployed public-key primitive — ECDSA/EdDSA, ECDH, BLS, and pairings (KZG, BN254) — relies on the classical hardness of exactly these problems, so all of them must be **replaced** (larger keys do not help).
- **Grover's algorithm** only quadratically weakens hash functions, which is recoverable by longer outputs. So hashes (Keccak, SHA-2/3) largely **survive** and need no algorithm change.

This split — Shor breaks structured problems, Grover only halves hash strength — is the dividing line for the whole migration.

Running Shor against 256-bit curves needs a fault-tolerant machine with on the order of millions of physical qubits, far beyond today's hardware, so Q-Day timing is uncertain (mainstream: 2029–2035). Urgency is nonetheless real because of (1) **"harvest-now, decrypt-later"** — recorded encrypted data can be decrypted after Q-Day, so exposure accrues today — and (2) the multi-year lead time to migrate the chain and its ecosystem.

Standards are ready to follow: NIST has finalized ML-KEM (FIPS 203), ML-DSA (FIPS 204), and SLH-DSA (FIPS 205), with FN-DSA (Falcon) in final publication; hybrid post-quantum key exchange is already deployed in mainstream TLS.

### What is vulnerable on BNB Chain

| Component                 | Cryptography                         | Consequence of a quantum break                               | Nature of harm                      |
| ------------------------- | ------------------------------------ | ------------------------------------------------------------ | ----------------------------------- |
| P2P networking (RLPx)     | ECDH key exchange                    | Recorded traffic decrypted later; metadata exposed           | Privacy (harvest-now-decrypt-later) |
| Account & tx signatures   | ECDSA (secp256k1)                    | Private keys derived from exposed public keys; assets stolen | Asset security (most severe)        |
| Consensus fast finality   | BLS aggregate signatures (BLS12-381) | Consensus forged; no mature post-quantum aggregation exists  | Consensus security                  |
| L2 / rollup commitments   | KZG (BLS12-381)                      | Data commitments forged; blob data already public            | Data integrity                      |
| Cryptographic precompiles | BN254 pairing (0x06–0x08)            | On-chain zk/BLS verification fails; forged proofs accepted   | Application security                |

The last three all rely on **pairing-friendly curves**, share one vulnerability, and are migrated together as a single "pairing family" — hence one phase, not three.

This table is the family-wide view; which rows apply to which chain, and what each chain adds beyond it, is broken down in [Section 5.1](#51-per-chain-cryptographic-surface).

## 4. Specification

> This umbrella BEP specifies only the roadmap and shared principles. Algorithms, parameters, transaction formats, and activation logic are defined in the companion Standards BEPs.

### Phase 1 — P2P key-exchange upgrade

Replace the elliptic-curve key exchange in node communication with a **hybrid post-quantum key exchange**, closing the harvest-now-decrypt-later exposure. Highest priority and most mature: a node-software upgrade only — no consensus, user, or address impact — and equivalent hybrid schemes are already proven in TLS. Execute first. *Details in the Phase 1 Standards BEP.*

### Phase 2 — ECDSA account-signature upgrade

Introduce **post-quantum account signatures alongside ECDSA**, with an address-preserving migration path. This is the largest asset-security surface: because addresses are reused, any address that has transacted has an exposed public key. Migration is opt-in and gradual — an account binds a new post-quantum key using its existing key, keeping the address unchanged — prioritizing high-value, already-exposed accounts. Note that post-quantum signatures are tens of times larger than ECDSA, pressuring bandwidth, storage, and performance; scheme selection weighs this. The same phase also covers the other plain elliptic-curve signing keys across the family — such as opBNB's sequencer, batcher and proposer keys — which share ECDSA's vulnerability without sharing its address-preservation constraint. *Details in the Phase 2 Standards BEP.*

### Phase 3 — Pairing-based cryptography upgrade

Migrate the **pairing family** — BLS aggregate signatures (fast finality), KZG commitments (L2/rollup data), and BN254 pairing precompiles — to post-quantum (hash-based) alternatives. This is the hardest, longest phase:

- **BLS aggregation** has no mature post-quantum equivalent (an industry-wide open problem); candidate directions include hash-based proofs of validator votes or standardized lattice aggregate/threshold signatures.
- **KZG** can move to hash-based (FRI/STARK-style) commitments, which are post-quantum and also remove the trusted setup, at the cost of larger sizes.
- **BN254 precompiles** migrate with the family toward hash-based verification.

Because the aggregation problem is research-gated, this phase is **"start when research-ready," with no fixed activation date**; crypto-agility keeps these components swappable so the eventual solution can be adopted without further disruptive forks. *Details in the Phase 3 Standards BEP(s) — possibly more than one, given the three distinct components.*

### Roadmap at a glance

| Phase | Component(s)                                   | Priority / timing                                  | Direction                                  |
| ----- | ---------------------------------------------- | -------------------------------------------------- | ------------------------------------------ |
| 1     | P2P key exchange                               | Highest — do first; mature, software-only          | Hybrid post-quantum key exchange           |
| 2     | ECDSA account signatures                       | Mid-term — opt-in, gradual, address-preserving     | Post-quantum signatures, hybrid with ECDSA |
| 3     | BLS aggregation, KZG, BN254 ("pairing family") | Long-term — hardest; research-gated, no fixed date | Hash-based / post-quantum replacements     |

Cross-cutting throughout: hybrid deployment, crypto-agility, address preservation, NIST alignment.

## 5. Per-Chain Migration Tracking

BNB Chain is not a single chain. **BSC** (L1), **opBNB** (OP-Stack L2), **BNB Greenfield** (CometBFT-based decentralized storage), and the **new chain currently under development** each have a distinct cryptographic surface, client codebase, release cadence, and governance process. The three phases above define the *direction* for all of them, but not one shared activation schedule: Phase 1 on opBNB is a different change from Phase 1 on BSC, and Phase 3 carries far more weight on BSC than on opBNB.

Therefore each chain publishes and maintains its **own PQC migration roadmap**, anchored to the phases and cross-cutting principles of this BEP. For chains governed through this repository, that roadmap is a companion BEP; for chains with a separate specification process, it is the equivalent tracking document in that process. This section is the **index and status tracker** for those roadmaps and is expected to be updated as sub-roadmaps are published and phases activate.

Two rules keep the family coherent:

1. **No divergence on primitives.** Algorithms, key encodings, signature envelopes, address-binding schemes, and hybrid-combiner constructions are defined once in the phase Standards BEPs and reused by every chain. A per-chain roadmap may schedule differently, scope differently, or skip an inapplicable phase, but it may not introduce a different PQC scheme for the same purpose. Any necessary deviation is recorded in [Section 5.4](#54-deviations).
2. **Shared components migrate jointly.** Where two chains verify each other's cryptography (see [Section 5.3](#53-cross-chain-dependencies)), neither can complete the migration alone; those upgrades are scheduled as coordinated, cross-chain hardforks.

### 5.1 Per-chain cryptographic surface

| Chain | Consensus / attestation | Accounts & tx | P2P / transport | Other Shor-vulnerable components | Phases that apply |
| --- | --- | --- | --- | --- | --- |
| **BSC** | Parlia PoSA; BLS12-381 aggregate votes for fast finality (BEP-126) | ECDSA (secp256k1) | RLPx (secp256k1 ECDH) | KZG blob commitments (BEP-336); BN254 pairing precompiles (0x06–0x08); EIP-2537 BLS12-381 precompiles once activated (BEP-439); CometBFT light-client and BLS relayer verification for cross-chain channels (BEP-221, BEP-682) | 1, 2, 3 (all) |
| **opBNB** | No independent BFT signature scheme; sequencer signs unsafe blocks with secp256k1; safety derives from BSC settlement | ECDSA (secp256k1), EVM-equivalent with BSC | libp2p Noise (X25519 ECDH) for block gossip | Batcher / proposer / challenger keys are ECDSA EOAs on BSC; BN254 precompiles inherited via EVM equivalence; DA commitments where blobs are used. Withdrawal proofs and fault proofs are Merkle/hash-based and therefore Grover-only — they survive | 1, 2; 3 partial (precompiles, DA) |
| **BNB Greenfield** | TBD | TBD | TBD | TBD | TBD |
| **New chain** (in development) | TBD — see note below | TBD | TBD | TBD | Design-time, not migration |

**Note on the new chain.** It is the only chain in the family with no deployed accounts, no address space to preserve, and no legacy client to keep compatible — which makes it by far the cheapest place to be quantum-resistant, and the only one where the cost is design effort rather than migration. Its roadmap is therefore not a phase schedule but a set of **launch requirements**:

- **Crypto-agility from genesis.** Every key, signature, and commitment carries an explicit algorithm identifier, and the protocol supports adding a new scheme without a disruptive fork or an address-format break.
- **Hybrid post-quantum transport from day one.** Phase 1 is a launch requirement, not a later upgrade — there is no reason to accumulate harvest-now-decrypt-later exposure in a chain that has not launched.
- **Account signatures chosen for PQC extensibility.** The signature and address derivation scheme must admit a post-quantum scheme being bound to an existing account later, on the Phase 2 model, so the chain never faces BSC's exposed-public-key problem retroactively.
- **No new pairing dependencies without an escape hatch.** If BLS aggregation or KZG-style commitments are used, they sit behind versioned interfaces so the eventual Phase 3 answer can be adopted in place.
- **Alignment with the shared primitives** defined in the phase Standards BEPs, so the new chain does not fork the family's cryptography.

These requirements are tracked here until the chain publishes its own specification, at which point it is added to [Section 5.2](#52-roadmap-status-index) like the others.

### 5.2 Roadmap status index

Status values: **Not started** · **Drafting** · **Specified** (roadmap/BEP published) · **In development** · **Activated** · **N/A**.

| Chain | Roadmap document | Phase 1 (P2P) | Phase 2 (accounts) | Phase 3 (pairing family) |
| --- | --- | --- | --- | --- |
| BSC | TBD | Not started | Not started | Not started (research-gated) |
| opBNB | TBD | Not started | Not started | Not started (research-gated) |
| BNB Greenfield | TBD | TBD | TBD | TBD |
| New chain | TBD | Launch requirement — not started | Launch requirement — not started | Deferred, behind versioned interfaces |

This table is the canonical status view for the family. It is updated whenever a per-chain roadmap is published, a phase specification reaches a milestone, or a phase activates on a chain; the corresponding BEP numbers replace the `TBD` entries as they are assigned.

### 5.3 Cross-chain dependencies

The chains are not independent, and three couplings constrain the ordering:

- **opBNB depends on BSC.** opBNB settles to BSC, and its batcher, proposer and challenger act through ECDSA accounts on BSC. opBNB's asset security is therefore bounded above by BSC's Phase 2 — completing Phase 2 on opBNB alone does not make opBNB assets quantum-safe. BSC leads; opBNB follows.
- **Cross-chain channels are mutually verified.** Where two chains verify each other's consensus cryptography — the BSC-side light-client and relayer verification used by the cross-chain channels (BEP-221, BEP-682, BEP-293) — neither side can migrate unilaterally: changing the scheme on one chain requires a matching change in the verifying contracts or precompiles on the other. Such upgrades are scheduled as **coordinated hardforks across both chains**. Scope and timing are TBD, pending the per-chain roadmaps of the chains involved.
- **Shared implementation surface.** All chains ultimately depend on the same PQC library selections, key formats and hybrid combiners. Divergent implementations of the same primitive would create a class of consensus and cross-chain verification bugs that is expensive to find and worse to fix, so the phase Standards BEPs are the single source, per rule 1 above.

A consequence worth stating plainly: the family's overall quantum resistance is determined by its **weakest verified link**, not its most advanced chain — a fully migrated opBNB still settles to BSC, and any chain that verifies another's consensus inherits that chain's exposure.

### 5.4 Deviations

Per-chain deviations from the shared primitives or principles — where a chain's constraints genuinely require a different construction — are recorded in this section with the rationale, so the family's actual cryptographic posture stays readable in one place. *None at time of writing.*

## 6. Rationale

**Order.** Sequencing follows threat shelf-life and solution maturity, not raw difficulty: Phase 1 first (threat accrues today, fix is mature and non-disruptive), Phase 2 next (largest asset surface, clear standardizable answer, gradual rollout), Phase 3 last (contains the one open research problem — post-quantum aggregation — so a fixed schedule would be premature).

**Multiple BEPs.** The work spans very different subsystems with different stakeholders and maturity levels. Splitting the roadmap (this Information BEP) from the per-phase technical specifications (companion Standards BEPs) lets each phase progress, gather review, and activate independently while staying anchored to shared principles — rather than coupling a ready-to-ship change (Phase 1) to a research-gated one (Phase 3).

**Per-chain roadmaps instead of one schedule.** The same argument applies across chains. The chains in the family share the threat model but not the cryptography: opBNB has no BFT signature scheme of its own to migrate, only BSC carries the full pairing family, and the chains differ in which signature and transport primitives they even use. Forcing one activation schedule would either hold back the chains that are ready or rush the ones that are not. Keeping the primitives centralized (this BEP and the phase Standards BEPs) while decentralizing the schedules (per-chain roadmaps, tracked in [Section 5](#5-per-chain-migration-tracking)) gets independent progress without cryptographic fragmentation. The new chain is separated further still, because for an unlaunched chain this is a design decision rather than a migration.

## 7. Ecosystem Considerations

Chain-level quantum resistance does not automatically protect applications. Bridges, stablecoin/RWA issuers, DAO treasuries and multisigs, wallets, oracles, and any contract verifying signatures or pairing-based proofs each carry their own vulnerable assumptions. Two cannot be fully fixed after the fact: **non-upgradeable contracts** with hard-coded vulnerable cryptography (design in crypto-agility before deployment, or plan migration to successor contracts), and **existing encrypted data** (irreversible harvest-now-decrypt-later risk). Core-protocol BEPs are delivered by the core team; ecosystem teams are expected to prepare in parallel, and the core team intends to publish migration standards, reference implementations, and guidance — cross-chain security is only as strong as the weakest connected component.

## 8. Backward Compatibility

Every phase runs in hybrid mode, coexisting with the existing classical mechanisms during transition rather than replacing them abruptly. Detailed compatibility analysis (transaction formats, node-software compatibility, wind-down of legacy channels) is provided per phase in the companion Standards BEPs, and per chain in the roadmaps tracked in [Section 5](#5-per-chain-migration-tracking). Phases affecting consensus, the economic model, or UX compatibility follow the standard BNB Chain governance process, including a governance vote where required; upgrades to mutually verified cross-chain components are activated as coordinated hardforks on both chains, so neither side ever verifies a scheme the other has already retired.

## 9. License

All the content is licensed under [CC0](https://creativecommons.org/publicdomain/zero/1.0/).