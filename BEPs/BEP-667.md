<pre>
  BEP: 667
  Title: Introduce Vote Interval to Relax Fast Finality Consensus Constraint
  Status: Draft
  Type: Standards
  Created: 2026-03-02
  Description: Relax Fast Finality from per-block voting to per-N-block voting, breaking the BlockInterval > 2×OWD physical limit.
</pre>

# BEP-667: Introduce Vote Interval to Relax Fast Finality Consensus Constraint

- [BEP-667: Introduce Vote Interval to Relax Fast Finality Consensus Constraint](#bep-667-introduce-vote-interval-to-relax-fast-finality-consensus-constraint)
  - [1. Summary](#1-summary)
  - [2. Abstract](#2-abstract)
  - [3. Motivation](#3-motivation)
  - [4. Specification](#4-specification)
    - [4.1 Vote Interval Parameter](#41-vote-interval-parameter)
    - [4.2 Modified Validator Vote Rules](#42-modified-validator-vote-rules)
    - [4.3 Modified Aggregate Vote Rules](#43-modified-aggregate-vote-rules)
    - [4.4 Modified Finality Rules](#44-modified-finality-rules)
  - [5. Performance Analysis](#5-performance-analysis)
  - [6. Reward and Slash](#6-reward-and-slash)
  - [7. Security](#7-security)
  - [8. Backward Compatibility](#8-backward-compatibility)
  - [9. License](#9-license)

## 1. Summary

This BEP introduces a **Vote Interval (N)** parameter to the Fast Finality mechanism ([BEP-126](./BEP126.md)), relaxing the consensus constraint from per-block voting to per-N-block voting. This breaks the fundamental `BlockInterval > 2 × OWD` physical limit, maximizing the time window available for transaction packing. **The initial value of N is 2.**

## 2. Abstract

Under the current Fast Finality mechanism (BEP-126), validators must complete a full vote-propagation cycle within each block interval, imposing a hard physical constraint. This BEP introduces a configurable Vote Interval `N` — validators only vote on every Nth block — relaxing the constraint to:

```
N × BlockInterval > 2 × OWD + T_import
```

where `OWD` (One-Way Delay) is the one-way network delay between validators and `T_import` is the block import/verification time. The current per-block voting model is `N = 1`. By increasing N, the minimum block interval drops to `(2 × OWD + T_import) / N`, breaking through the physical barrier of per-block voting.

## 3. Motivation

The current block interval of 450ms on BSC ([BEP-619](./BEP-619.md)) is approaching the physical limit imposed by Fast Finality's per-block voting requirement.

BEP-126 assumes that "1 block time is totally enough for validators to receive a block, vote for the block and propagate the vote to other validators." This assumption requires:

```
BlockInterval > 2 × OWD + T_import
```

Block propagation from the proposer to validators takes one `OWD` (~120ms), and vote propagation back takes another `OWD`. With `T_import ≈ 30ms`, the minimum block interval for per-block voting is `2 × 120 + 30 = 270ms`. The current 450ms interval satisfies this, but since the proposer imports the parent block in parallel with waiting for votes, the effective per-block overhead is `2 × OWD = 240ms`, and vote propagation dominates the time budget:

```
|<------------ BlockInterval (450ms) ------------>|
|  T_const  |           T_fill (~210ms)           |
|  (~240ms) |                                     |

CPU Utilization ≈ T_fill / BlockInterval = 210 / 450 ≈ 46.7%
```

More than half of each block interval is consumed by consensus overhead rather than transaction processing. As block intervals decrease further, this ratio worsens — the constant `2 × OWD` overhead takes an ever-larger share.

By voting every N blocks, the `2 × OWD` overhead is amortized across N intervals, dramatically increasing the fraction of time available for packing.

## 4. Specification

### 4.1 Vote Interval Parameter

Introduce a new consensus parameter:

```
VoteInterval = N    (initial value: 2)
```

where `N ≥ 1` is a positive integer. When `N = 1`, the behavior is identical to the current per-block voting model. The value of N is governable through the BSC governance process to adapt to changing network conditions.

A block is defined as a **voting block** if:

```
blockNumber % N == 0
```

Only voting blocks participate in the Fast Finality vote-aggregate-justify cycle. Non-voting blocks are produced and validated normally but do not carry vote attestations in their headers and do not trigger voting.

### 4.2 Modified Validator Vote Rules

The vote rules from BEP-126 Section 4.1.1 are adapted to operate on voting blocks:

1. Validators only cast votes for **voting blocks** (`blockNumber % N == 0`).
2. The source `s` and target `t` of a vote message `<v, s, t, h(s), h(t)>` refer to voting blocks only, where `h()` denotes the voting block's sequence number (i.e., `blockNumber / N`).
3. All existing vote rules apply to the voting block sequence:
   - **Rule 1**: A validator must not publish two distinct votes for the same voting block height.
   - **Rule 2**: A validator must not vote within the span of its other votes.
   - **Rule 3**: Validators always vote for their canonical chain's latest voting block.

### 4.3 Modified Aggregate Vote Rules

The aggregate vote rules from BEP-126 Section 4.1.2 are modified as follows:

1. When a validator proposes a **voting block** (i.e., `blockNumber % N == 0`), if the previous voting block (at `blockNumber - N`) has gathered ⅔+ votes from valid validators, the proposer aggregates these votes as an attestation for that previous voting block.
2. The attestation is set into the extra field of the voting block's header.
3. Non-voting blocks (`blockNumber % N != 0`) MUST NOT carry vote attestations in their headers.

### 4.4 Modified Finality Rules

The finality rules from BEP-126 Section 4.1.3 are modified to operate on voting blocks:

1. A voting block is called **justified** if:
   - (1) it is the root, or
   - (2) there exists an attestation for this block in the next voting block's header.
2. A voting block is called **finalized** if:
   - (1) it is the root, or
   - (2) it is justified and the next voting block after it is also justified.
3. When a voting block is finalized, **all blocks between it and the previous finalized voting block are also considered finalized**.

## 5. Performance Analysis

Assuming `OWD ≈ 120ms`, `T_import ≈ 30ms`, feasibility requires `N × BlockInterval > 2 × OWD + T_import = 270ms`.

With `N = 1` (per-block voting), `T_fill = BlockInterval - 2 × OWD` (the proposer imports the parent block in parallel with vote propagation, so only `2 × OWD` is subtracted). With `N ≥ 2`, once the constraint is satisfied, vote propagation runs in the background across N intervals, and `T_fill = BlockInterval - T_import`.

Each N value unlocks a lower minimum block interval: `BlockInterval_min = (2 × OWD + T_import) / N`. The following table shows each N at the block interval it uniquely enables:

| BlockInterval | N | T_fill | CPU Util | Finality Latency | Throughput (T_fill/s) |
|---------------|---|--------|----------|------------------|-----------------------|
| 450ms | 1 | 210ms | 46.7% | 900ms | 467ms/s |
| 250ms | 2 | 220ms | 88% | 1000ms | 880ms/s |
| 100ms | 3 | 70ms | 70% | 600ms | 700ms/s |

At the same block interval, increasing N beyond the minimum feasible value provides no additional benefit — only worse finality latency. The optimal N is the **smallest value that satisfies the constraint** for a given block interval.

## 6. Reward and Slash

### 6.1 Reward Adjustment

Under BEP-126 and BEP-319, validators are rewarded for votes that are wrapped into attestations. With Vote Interval, voting occurs every N blocks instead of every block, reducing the number of reward events per epoch by a factor of N.

To maintain equivalent validator incentives:
- The **reward-per-attestation** should be scaled by a factor of N, so that total rewards per epoch remain unchanged.
- The reward distribution logic at epoch boundaries (BEP-319) requires no structural changes — only the per-attestation weight is adjusted.

### 6.2 Slash Rules

The slash rules from BEP-126 Section 4.4 apply to voting blocks:
- **Double vote**: A validator publishing two distinct votes for the same voting block height is slashable.
- **Surround vote**: A validator voting within the span of its other votes on the voting block sequence is slashable.
- The evidence submission deadline (BEP-319 Section 4.3) should be adjusted to account for the reduced number of voting blocks per unit time.

## 7. Security

This BEP does not alter the vote rules, finality thresholds, or cryptographic assumptions of BEP-126. The voting block subsequence satisfies the same invariants — identical vote rules, same ⅔+ threshold — so the proofs of **Accountable Safety** (Theorem 1) and **Plausible Liveness** (Theorem 2) apply directly.

Block production liveness is unaffected: non-voting blocks do not require Fast Finality participation, and the chain can grow with ½+1 validators online as before.

## 8. Backward Compatibility

This BEP is **not backward compatible**. It changes the Fast Finality voting and aggregation rules, requiring a hard fork with coordinated client upgrades across all validators and full nodes.

Fast finality latency increases from `2 × BlockInterval` to `2 × N × BlockInterval`, as justification and finalization operate on the voting block subsequence.

## 9. License

The content is licensed under [Creative Commons CC0 1.0 Universal License](https://creativecommons.org/publicdomain/zero/1.0/).
